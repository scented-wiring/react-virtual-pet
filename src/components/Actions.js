import "../styles/Actions.css";

const Actions = () => {
  return (
    <div class="Actions">
      <button>Feed</button>
      <button>Play</button>
      <button>Clean</button>
    </div>
  );
};

export default Actions;
